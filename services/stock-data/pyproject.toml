[project]
name = "stock-data"
version = "0.1.0"

requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.6",
    "pathlib>=1.0.1",
    "psycopg2-binary>=2.9.13",
    "yfinance>=1.7.0",
]

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
